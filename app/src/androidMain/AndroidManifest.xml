<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:name=".StatsCounterApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Stats Counter"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.StatsCounter">
        <!-- configChanges: orientation|screenSize は AppNavigationEffect が requestedOrientation を
             切り替える際のアクティビティ再起動を抑制するための回避策。
             requestedOrientation を廃止する際は orientation|screenSize も同時に除去すること。 -->
        <activity
            android:name=".MainActivity"
            android:configChanges="keyboardHidden|orientation|screenSize"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>
